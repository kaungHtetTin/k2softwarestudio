<IfModule mod_rewrite.c>
    RewriteEngine On
    # If the app lives in a subdirectory, set RewriteBase to that path (e.g. /k2/public/)
    # RewriteBase /k2/public/

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
